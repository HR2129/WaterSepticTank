import { Warehouse } from "lucide-react";

const config = {
  source: "WEB", // Static source for the web application
  deptId: "1221", // Default department ID
 
};

export default config; // Export the config object for use in other files
